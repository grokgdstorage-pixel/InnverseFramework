# reference/operations/

How humans and Grok produce play text.

Kit file:
- `play-packet.md` — which vault pages to open, what to return, what not to invent

These rules are inherited by every clone. Change them in the template only when the method itself changes. A single game’s posting exceptions go in `reference/campaign/`.
